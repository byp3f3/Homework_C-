<Window x:Class="tests.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tests"
        mc:Ignorable="d"
        Title="Тест" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Frame x:Name="PageFrame" Grid.Row="1" Grid.ColumnSpan="3" NavigationUIVisibility="Hidden"/>
        <Button x:Name="_out" Content="Выйти" Click="Button_Click" Background="Pink" />
        <Button x:Name="changeTest" Grid.Column="1" Content="Редактор теста" Click="changeTest_Click" Background="Pink"/>
        <Button x:Name="takeTest" Grid.Column="2" Content="Пройти тест" Click="takeTest_Click" Background="Pink" />
    </Grid>
</Window>
