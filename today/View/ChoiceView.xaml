<UserControl x:Class="today.View.ChoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:today.View"
                      mc:Ignorable="d" 
         d:DesignHeight="80" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition />
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <CheckBox x:Name="chosen" VerticalAlignment="Center"/>
        <Image x:Name="icon" VerticalAlignment="Center"/>
        <TextBlock x:Name="nameTbk" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
